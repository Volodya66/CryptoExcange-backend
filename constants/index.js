const EMAILREGEXP = require("./emailRegexp");
const { CRYPTO_ENUM_SYMBOLS, CRYPTO_ENUM } = require("./namesCryptocurrencies");
module.exports = {
  EMAILREGEXP,
  CRYPTO_ENUM_SYMBOLS,
  CRYPTO_ENUM,
};
